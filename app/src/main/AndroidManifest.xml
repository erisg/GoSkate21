<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="skate.bogota.goskate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="skate.bogota.authgoskate.ui.Login"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name="skate.bogota.authgoskate.ui.NewUser"
            android:windowSoftInputMode="stateHidden"/>


        <activity android:name="skate.bogota.goskate.RecyclerViews.HomeSkate"
            android:windowSoftInputMode="stateHidden"/>
    </application>

</manifest>